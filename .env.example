ANTHROPIC_API_KEY=your_api_key_here

# Optional: protect the app with a password (leave blank for open access)
APP_PASSWORD=

# Optional: path to the SQLite session database (default: sessions.db in project root)
AI2_SESSION_DB=sessions.db
